[package]
name = "accounting-assassin"
version = "0.1.0"
description = "记账杀手 — a learning app for using AI coding tools"
edition = "2021"
rust-version = "1.77.2"

[build-dependencies]
tauri-build = { version = "2.6.2", features = [] }

[dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.11.2", features = [] }
tauri-plugin-fs = "2"
